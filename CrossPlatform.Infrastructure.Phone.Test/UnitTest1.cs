using System;
using Microsoft.VisualStudio.TestPlatform.UnitTestFramework;

namespace CrossPlatform.Infrastructure.Phone.Test
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void MsgBoxTest()
        {
            // Arrange (시나리오를 설정한다)

            // Act (작업을 시도한다) 

            // Assert (결과를 검증한다)
        }

        [TestMethod]
        public async void ConfirmTest()
        {
            // Arrange (시나리오를 설정한다)
            
            // Act (작업을 시도한다) 

            // Assert (결과를 검증한다)
        }
    }
}
